import { styled } from 'styled-components'

export const RequiredMessageContainer = styled.span`
  color: var(--red);
  font-size: 12px;
  font-weight: 700;
  display: block;
  margin-bottom: 20px;
`
